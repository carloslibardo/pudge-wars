// Loaded by content/panorama/layout/custom_game/custom_ui_manifest.xml, which
// is the single entry point the engine reads for custom UI. Anything not
// reachable from that manifest never loads.
$.Msg("[UI] manifest loaded");
