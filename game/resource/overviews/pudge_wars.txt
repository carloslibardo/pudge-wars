// Minimap world-box registration (2026-08-02). Values come from
// mapgen/genoverview.py: the texture covers a square of half-extent
// max(halfWidth, halfHeight) + rim = 2856 centered on the origin.
// pos_x/pos_y are the world coordinates of the texture's TOP-LEFT corner;
// scale is world units per texture pixel (5712 / 1024).
"pudge_wars"
{
	// Run 34: the bare "overviews/pudge_wars" form logged
	// `FixupResourceName: Illegal path, missing extension` and the minimap
	// stayed unpainted — Source 2 wants the full materials/ path WITH .vmat.
	"material"	"materials/overviews/pudge_wars.vmat"
	// Stock overview txts also name a simple_material; alias ours to the
	// same vmat in case the HUD's minimap picks the simple variant.
	"simple_material"	"materials/overviews/pudge_wars.vmat"
	"pos_x"		"-2856"
	"pos_y"		"2856"
	"scale"		"5.578125"
}
