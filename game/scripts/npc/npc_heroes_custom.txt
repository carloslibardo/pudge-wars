// Base heroes overridden IN PLACE. Everything you do not restate is inherited
// from the stock hero, so this file only lists the deltas.
"DOTAHeroes"
{
	"npc_dota_hero_pudge"
	{
		// The block key IS the base hero name — no "override_hero" (landmine L2).
		// Everyone is Pudge, forced in GameMode with SetCustomGameForceHero,
		// and GetUnitName() returns this base name for every player.

		// Hook-only mode: strip the right-click so every kill flows through Meat
		// Hook + Rot. NO_ATTACK is what actually removes the attack; the
		// 1-damage floor is a fallback for anything that force-attacks.
		"AttackCapabilities"			"DOTA_UNIT_CAP_NO_ATTACK"
		"AttackDamageMin"				"1"
		"AttackDamageMax"				"1"

		// Ability slots are filled as each ability lands (specs 002-003).
		"Ability1"						"pudge_meat_hook"
		"Ability2"						""
		"Ability3"						""
		"Ability4"						""
		"Ability5"						""
		"Ability6"						""
	}
}
