//
// <key>	<value>   — 1 enables the hero for this custom game.
//
// Base hero name, deliberately. Everyone is Pudge (SetCustomGameForceHero),
// and a "_custom" name would not resolve — the engine would hand out a random
// stock hero at the selection timeout, silently. Overridden in place in
// npc_heroes_custom.txt, so GetUnitName() returns this base name forever.
"CustomHeroList"
{
	"npc_dota_hero_pudge"			"1"
}
