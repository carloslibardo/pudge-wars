// Custom abilities. Each ability key must EXACTLY match its exported class name
// in src/vscripts/abilities/, and each ScriptFile is that module's path relative
// to game/scripts/vscripts/ (no extension). Balance numbers live in
// AbilityValues and are read in TS with GetSpecialValueFor — never hardcoded.
"DOTAAbilities"
{
}
